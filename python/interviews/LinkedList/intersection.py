"""
Given two linkedlist, determin if two lists intersect. Return the intersecting node.
Note that intersection os defined based on referenc, not value. That is, if kth node of 1st
is the exactly same  as teh jth of 2nd, then they are intersecting
"""